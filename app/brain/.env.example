# Deprecated — use the root compose at ../../docker-compose.yml
# Kept only so local tooling does not surprise you.
#
# Prefer:
#   cd ../.. && npm run docker:up
#
# Or:
#   docker compose -f ../../docker-compose.yml up -d --build

PROJECT_NAME=mosi-mosi
DOMAIN=localhost
WEB_URL=http://localhost:3020
API_URL=http://localhost:3011
POSTGRES_USER=mosi
POSTGRES_PASSWORD=mosi
POSTGRES_DB=mosi
POSTGRES_PORT=5433
API_PORT=3011
DATABASE_URL=postgresql://mosi:mosi@localhost:5433/mosi
